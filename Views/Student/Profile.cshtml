@model Student

@{
    Layout = "~/Views/Shared/DashboardLayout.cshtml";
    ViewBag.Title = "My Profile";
}

<div class="container py-4">
    <div class="card border-0 shadow-sm overflow-hidden">
        <div class="card-header bg-gradient-primary text-white p-4">
            <h3 class="mb-0">My Student Profile</h3>
        </div>
        <div class="card-body p-4 row">
            <div class="col-md-4 text-center border-end">
                <div class="position-relative d-inline-block">
                    <img src="@(string.IsNullOrEmpty(Model.ProfileImagePath) ? "/Images/default-avatar.png" : "/" + Model.ProfileImagePath)" 
                         class="img-thumbnail rounded-circle mb-3 shadow-sm" style="width: 200px; height: 200px; object-fit: cover;" 
                         onerror="this.src='/Images/admin.png'; this.onerror=null;" alt="Profile Image" />
                </div>
                <h4 class="fw-bold mt-3">@Model.FirstName @Model.LastName</h4>
                <p class="text-muted">@@@Model.User?.Username</p>
                <span class="badge bg-primary px-3 py-2 rounded-pill">Student</span>
            </div>
            <div class="col-md-8">
                <div class="ps-md-4">
                    <h5 class="text-uppercase letter-spacing-1 small fw-bold text-muted mb-4">Account Information</h5>
                    <div class="row g-3">
                        <div class="col-sm-6">
                            <label class="small text-muted mb-1">Email Address</label>
                            <p class="fw-medium">@Model.Email</p>
                        </div>
                        <div class="col-sm-6">
                            <label class="small text-muted mb-1">Phone Number</label>
                            <p class="fw-medium">@(Model.PhoneNumber ?? "Not provided")</p>
                        </div>
                        <div class="col-sm-6">
                             <label class="small text-muted mb-1">Gender</label>
                            <p class="fw-medium">@Model.Gender</p>
                        </div>
                        <div class="col-sm-6">
                            <label class="small text-muted mb-1">Date of Birth</label>
                            <p class="fw-medium">@Model.DateOfBirth.ToString("MMM dd, yyyy")</p>
                        </div>
                    </div>

                    <hr class="my-4 opacity-50" />

                    <h5 class="text-uppercase letter-spacing-1 small fw-bold text-muted mb-4">Academic Details</h5>
                    <div class="row g-3">
                        <div class="col-sm-6">
                            <label class="small text-muted mb-1">Grade / Class</label>
                            <p class="fw-medium">@Model.Grade</p>
                        </div>
                        <div class="col-sm-6">
                            <label class="small text-muted mb-1">Enrollment Date</label>
                            <p class="fw-medium">@Model.EnrollmentDate.ToString("MMM dd, yyyy")</p>
                        </div>
                        <div class="col-sm-6">
                            <label class="small text-muted mb-1">Assigned Instructor</label>
                            <p class="fw-medium">@(Model.Instructor?.FirstName + " " + Model.Instructor?.LastName ?? "None")</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <div class="card-footer bg-light p-3 text-end">
            <a asp-action="Settings" asp-controller="Account" class="btn btn-outline-dark me-2">
                <i class="fas fa-cog me-1"></i> Settings
            </a>
            <a asp-action="Dashboard" class="btn btn-outline-secondary me-2">Back to Dashboard</a>
            <button class="btn btn-success px-4" onclick="Swal.fire('Edit Profile', 'Please contact the system administrator to update your professional details.', 'info')">
                <i class="fas fa-edit me-2"></i> Request Change
            </button>
        </div>
    </div>
</div>
